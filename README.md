useCallback

  O useCallback é um hook do React que memoiza uma função, garantindo que a mesma referência de função seja mantida entre renderizações, a menos que suas dependências mudem. 

  Em outras palavras, o useCallback "memoriza" a função para que ela não seja recriada a cada renderização do componente
